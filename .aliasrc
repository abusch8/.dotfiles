#!/bin/bash


case "$0" in
    *bash*)
        shopt -s expand_aliases
        ;;
    *zsh*)
        setopt aliases
        ;;
esac

alias ls='ls --color=auto'
alias ll='ls --color=auto -l'
alias vi='nvim'

function gg() {
    git add --all
    git commit -m "$1"
    git push
}

alias gg=gg

